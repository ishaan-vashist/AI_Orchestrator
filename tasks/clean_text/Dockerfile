# Use a lightweight Python 3.10 base image
FROM python:3.10-slim

# Set working directory inside the container
WORKDIR /app

# Copy the task script into the container
COPY app.py .

# Define the default command to run the task
CMD ["python", "app.py"]
